
apiVersion: v1
kind: Service
metadata:
  name: go-app
  labels:
    app: go-app

spec:
  selector:
    app: go-app

  type: NodePort
  ports:
  - nodePort: 31000
    port: 8080
    targetPort: 8080